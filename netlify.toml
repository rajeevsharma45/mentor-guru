[build]
  command = "npm install -g pnpm@9.15.4 && pnpm install --frozen-lockfile && pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20.18.1"
  NETLIFY_USE_COREPACK = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"